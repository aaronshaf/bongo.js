/*
Model sync (Racer?)
WebRTC (peer)
*/

Holy grail:
  Real-time via WebSockets +
  Model synchronization +
  Peer synchronization +
  Robust querying +
  Fallback

Real-time via WebSockets
  
Model synchronization

Peer synchronization
  webrtc.io

Robust querying
  Smart utilization of indexes
  JSONiq?
  Full-text search?

Fallback
  Use REST API & localStorage/sessionStorage when necessary

Decentralization
  WebRTC: Sync changes with clients before syncing with master server.

Scenario

REST API's: Less for querying, more for syncing to local storage.
